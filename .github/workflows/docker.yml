name: Docker images

# Every image builds on every PR; only main and tags push. A PR build is the only thing that
# exercises giano-bundler, giano-devnet and giano-contracts-deployer at all — the e2e compose
# stack builds just wallet-api, wallet-web and paymaster-admin, and uses upstream images for
# the rest — so without this a broken Dockerfile first surfaces in the job that publishes it.
on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-${{ github.ref }}
  # superseded PR builds are cancelled; a push that may be mid-publish is never cancelled
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  images:
    name: Build ${{ matrix.image.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image:
          - name: giano-wallet-api
            dockerfile: services/wallet-api/Dockerfile
          - name: giano-wallet-web
            dockerfile: services/wallet-web/Dockerfile
          - name: giano-paymaster-admin
            dockerfile: services/paymaster-admin/Dockerfile
          - name: giano-bundler
            dockerfile: services/bundler/Dockerfile
          - name: giano-devnet
            dockerfile: services/devnet/Dockerfile
          - name: giano-contracts-deployer
            dockerfile: packages/contracts/Dockerfile.deployer
    steps:
      - uses: actions/checkout@v4
        with:
          # giano-contracts-deployer runs `hardhat compile`, and hardhat-foundry resolves
          # `solady` through foundry's libs auto-remapping from packages/contracts/lib/solady
          # — a submodule, not an npm dep. Without it: HH411, solady is not installed.
          submodules: recursive
      - uses: docker/setup-qemu-action@v3
        # binfmt for the emulated linux/arm64 leg
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        # no registry credentials on PRs — nothing is pushed, and a fork PR has none
        if: github.event_name != 'pull_request'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/appliedblockchain/${{ matrix.image.name }}
          tags: |
            type=ref,event=tag
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.image.dockerfile }}
          push: ${{ github.event_name != 'pull_request' }}
          # both arches on every event, PRs included: ECS runs ARM64 (infra/iac
          # ecs-service.tf, ecs_tasks_oneshot.tf), so arm64 is the leg that actually gets
          # deployed — validating only amd64 on a PR would skip the one that matters
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          # PRs read main's cache but never write: branch cache entries are invisible to main
          # and would only evict it from the repo's shared 10GB budget
          cache-to: ${{ github.event_name != 'pull_request' && 'type=gha,mode=max' || '' }}
